\ProvidesPackage{eForceStyle}
%----Packages only------
%-----------------------
%\usepackage{fontspec}
%\setmainfont[Mapping=text-tex]{Calibri}
\usepackage{hyperref} 
\usepackage{graphicx}
\usepackage{float}
\usepackage{tabularx}% sized tables
\usepackage{nameref}% section ref names
\usepackage{bigstrut}% tables

\usepackage{todonotes}% todos

\usepackage{pgfplots}%plots

\usepackage{pdfpages}%including whole pdf pages (datasheets,...)

\pgfplotsset{compat=newest} % Allows to place the legend below plot
\usepgfplotslibrary{units} % Allows to enter the units nicely
%-------------------------------------
\usepackage{appendix}% appendixy
%---------------------------------------------------
\usepackage[a4paper, total={17.01cm, 23.87cm}]{geometry}
%------------------acronym------------
\usepackage[acronym,nomain,nonumberlist,nogroupskip]{glossaries} % bez cisel
%\usepackage[acronym,nomain]{glossaries}
%\usepackage[acronym,nonumberlist]{glossaries}
%\usepackage[acronym]{glossaries}
%------------------header-------------
\usepackage{fancyhdr}
%----------------------------------------
%--------------NEW COMMANDS--------------
% Paragraph
	\renewcommand\paragraph{\@startsection{paragraph}{4}{\z@}%
		{-3.25ex\@plus -1ex \@minus -.2ex}%
		{1.5ex \@plus .2ex}%
		{\normalfont\normalsize\bfseries}}
% References
	\AtBeginDocument{\renewcommand{\ref}[1]{\autoref{#1}: \nameref{#1}}}
	%\AtBeginDocument{\renewcommand{\ref}[1]{\nameref{#1}}}
	%\AtBeginDocument{\renewcommand{\ref}[1]{\autoref{#1}}}
% TOC
	\renewcommand*\contentsname{Table of Contents}
% Names 
	\newcommand{\@CarNumber}[1]{\def\CarNumber{#1}}
% Fancy Header
	\pagestyle{fancy}
	\fancyhf{}
	\fancyhead[R]{\rightmark}
	\fancyhead[L]{\UniversityName, \CarNumber}
	\fancyfoot[l]{\Event}
	\fancyfoot[R]{\thepage}
	%\renewcommand{\headrulewidth}{1pt}
	\renewcommand{\footrulewidth}{.5pt}
	
	\pagenumbering{arabic}
% Section numbering - thhis is faked

% Size of table
	\newcommand{\TableSize}{.5cm}

%---author of template: Ondrej Sereda
%---Developed only for eForce purpose